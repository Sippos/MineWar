# MOV-017 World Core Manifest

Status: implemented locally on `refactor/mov-017-world-core`; not pushed or merged

Base branch: `origin/refactor/mov-016-boot-main-menu`

Verified base commit: `e4db065a0de9a767d1611f3c46465c5833a837a0`

## 1. Sub-batch boundary

This manifest covers only the first MOV-017 implementation sub-batch: relocation of the central mine scene and its world controller.

### Source → target

- `level.tscn` → `scenes/world/mine/level.tscn`
- `world.gd` → `scripts/systems/world_generation/world.gd`
- `world.gd.uid` → `scripts/systems/world_generation/world.gd.uid`

No terrain image, `.import` sidecar, TileSet extraction, node rename, resource-ID change, collision change, navigation change, rendering change, input change, wave change, or gameplay refactor is authorized.

The only files physically moved by this sub-batch are the three files above. Other touched files are limited to path references, generated path templates, current documentation, and the manifest/progress record.

## 2. Frozen identity contracts

- `level.tscn` scene UID: `uid://cads35kywyo0i`
- `world.gd.uid`: `uid://bmhoamkntdku5`
- `level.tscn` root: `Level` (`Node2D`)
- `level.tscn` root script before move: `res://world.gd`
- `level.tscn` root script after move: `res://scripts/systems/world_generation/world.gd`
- Configured project entry remains `res://launch_router.tscn`.
- Single-player wrapper remains `res://scenes/boot/main.tscn` and must instance the relocated level exactly once.

The scene UID and script UID must not change. The `world.gd.uid` sidecar must move with the script. No new UID is generated by hand.

## 3. Exact runtime and generated-path reference manifest

Every tracked direct path below must be updated atomically.

### `res://level.tscn` → `res://scenes/world/mine/level.tscn`

- `scenes/boot/main.tscn`
- `vs_mode.tscn`
- `vs_online.tscn`
- `run_test.gd`
- `test_show.gd`
- `test_show_menu.gd`
- `test_vs2.gd`
- `build_online.py` generated `vs_online.tscn` template
- `build_vs.py` generated `vs_mode.tscn` template
- `docs/refactor/VALIDATION_CHECKLIST.md`

`build_vs.py` currently declares stale generated UID `uid://dl0bto1orfw6t` for the level resource. Its template must use the frozen scene UID `uid://cads35kywyo0i` while updating the path.

### `res://world.gd` → `res://scripts/systems/world_generation/world.gd`

- moved `scenes/world/mine/level.tscn`
- `test_astar.gd`
- `test_path.gd`

### Repository-relative maintenance paths that genuinely target the moved files

These are path-only updates because the utilities directly open or name the current root files:

- `add_shadows.py`: `level.tscn`
- `fix_logic.py`: `world.gd`
- `fix_world.py`: `world.gd`
- `patch_vs.py`: `world.gd`
- `patch_world_and_level.py`: both `level.tscn` and `world.gd`
- `update_layout.py`: `world.gd`
- `update_world_25d.py`: `world.gd`
- `update_world_gd.py`: `world.gd`
- `update_world_mask.py`: `world.gd`

Their algorithms, output, and mutation behavior remain unchanged. Only the repository-relative target path changes.

## 4. Pre-existing stale tooling explicitly excluded

The following tracked patchers/probes currently target `scenes/boot/main.tscn` as though it contained the embedded world TileSet. MOV-016 made that path the thin gameplay wrapper, so these tools are already stale before MOV-017:

- `add_front_dmg_layer.py`
- `fix_edge_sources.py`
- `fix_edges_tscn.py`
- `fix_everything.py`
- `fix_top_tiles.py`
- `fix_tscn.py`
- `fix_tscn_assets.py`
- `fix_tscn_fronts.py`
- `remove_ysort.py`
- `test_tileset.gd`
- `update_final_fronts.py`
- `update_tileset.py`
- `update_tscn.py`
- `update_tscn_25d.py`

This sub-batch does not repair, execute, relocate, delete, or reclassify those historical utilities. Correcting their intended targets is a separate tooling-classification/repair task because doing so would change a pre-existing behavior rather than merely follow a moved file.

Validation must not treat these known stale wrapper targets as a MOV-017 regression.

## 5. Frozen external resources in the level scene

All current external resources remain at their existing paths except the world script path.

### Runtime scripts/scenes

- `res://player.gd`
- `res://map_bounds.gd`
- `res://base.tscn`
- `res://hud.tscn`
- `res://upgrade_menu.tscn`

### Terrain/rendering resources excluded from this sub-batch

- `res://solid-earthen-wall-block-tile-93e7.png`
- `res://Black_BG_TransparentBorder.png`
- `res://Easy_Brick.png`
- `res://Medium_Brick.png`
- `res://Hard_Brick.png`
- `res://Easy_Edge_Atlas.png`
- `res://Medium_Edge_Atlas.png`
- `res://Hard_Edge_Atlas.png`
- `res://First_Hitting.png`
- `res://Second_Hitting.png`
- `res://fog_mask_atlas.png`
- `res://Easy_Brick-Front.png`
- `res://Medium-Brick-Front.png`
- `res://Hard-Brick-Front.png`
- `res://First-Hit-Front.png`
- `res://Next-Hit-Front.png`
- `res://rail_trail_atlas.png`
- `res://Easy_Edge_Atlas-1-Stat-Ressources.png`
- `res://Stat_Ressources_Overlay_Front.png`

Their source files and `.import` sidecars must remain untouched.

## 6. Embedded TileSet contract

The TileSets remain embedded in `level.tscn`. No standalone `.tres` is created.

### Common geometry

- Tile size: `64 × 64`
- Main terrain physics collision layer: `1`
- Block occluder/light mask: `1`
- Block collision polygon: full `64 × 64` square

### `TileSet_main` source map

| Source ID | Embedded source | Texture/resource role | Atlas contract |
| ---: | --- | --- | --- |
| 0 | `Source_bg` | background | `(0,0)` |
| 1 | `Source_easy` | easy brick | `(0,0)`, collision |
| 2 | `Source_med` | medium brick | `(0,0)`, collision |
| 3 | `Source_hard` | hard brick/boundary | `(0,0)`, collision |
| 4 | `Source_edge_easy` | easy edge | 4×4 mask atlas |
| 5 | `Source_edge_med` | medium edge | 4×4 mask atlas |
| 6 | `Source_edge_hard` | hard edge | 4×4 mask atlas |
| 7 | `Source_dmg1` | first damage stage | `(0,0)` |
| 8 | `Source_dmg2` | second damage stage | `(0,0)` |
| 9 | `Source_fog` | fog mask | 4×4 mask atlas |
| 10 | `Source_front_easy` | easy front wall | `(0,0)`, texture origin `(0,32)` |
| 11 | `Source_front_med` | medium front wall | `(0,0)`, texture origin `(0,32)` |
| 12 | `Source_front_hard` | hard front wall | `(0,0)`, texture origin `(0,32)` |
| 13 | `Source_front_dmg1` | first front-damage stage | `(0,0)`, texture origin `(0,32)` |
| 14 | `Source_front_dmg2` | second front-damage stage | `(0,0)`, texture origin `(0,32)` |
| 15 | `Source_rail` | rail trail atlas | 4×4 mask atlas |

### Numerical consumers that must remain unchanged

- `world.gd`: block sources `1–3`, edge sources `4–6`, fog `9`, front walls `10–12`, rail `15`
- `player.gd`: damage sources `7–8`, front damage `13–14`
- `spider_minion.gd`: damage sources `7–8`, front damage `13–14`
- `map_bounds.gd`: boundary source `3`
- `scripts/gameplay/transport/minecart/minecart.gd`: rail source `15`
- `tests/test_collectible_characterization.gd`: rail source `15`

### Mask/atlas mapping

Fog, edge, and rail masks use cardinal bits:

- top = `1`
- right = `2`
- bottom = `4`
- left = `8`

Atlas coordinates remain `Vector2i(mask % 4, int(mask / 4))`. An isolated rail substitutes mask `5`. No source order, atlas coordinate, or cell data is rewritten.

## 7. Frozen node, rendering, and signal contracts

The scene hierarchy and node names are runtime API and must remain byte-equivalent apart from resource path lines.

### Direct children of `Level`

- `CanvasModulate`
- `MapBounds`
- `HUD`
- `UpgradeMenu`
- `Base`
- `BackgroundLayer`
- `BlockLayer`
- `RailLayer`
- `FrontWallLayer`
- `EdgeLayer`
- `DamageLayer`
- `FrontDamageLayer`
- `FogLayer`
- `Player`

### Rendering/order properties

- root `Level`: `y_sort_enabled = true`
- `BackgroundLayer`: `z_index = -5`
- `BlockLayer`: `y_sort_enabled = true`
- `RailLayer`: `z_index = -1`
- `FrontWallLayer`: `y_sort_enabled = true`
- `EdgeLayer`: `z_index = 1`
- `DamageLayer`: `z_index = 1`, `y_sort_enabled = true`
- `FrontDamageLayer`: `y_sort_enabled = true`
- `FogLayer`: `z_index = 10`
- world constant `FRONT_GEM_Z_INDEX = 2`

### Scene signal wiring

- `Base.gems_deposited` → `HUD.add_gems`
- `Base.upgrade_requested` → `UpgradeMenu.show_menu`
- `Base.base_damaged` → `HUD.update_health`
- `Base.game_over` → `HUD.on_game_over`

### World API used by VS and gameplay consumers

The move must preserve, without edits:

- exported `player_id`
- exported `is_vs_mode`
- `income`
- `ENEMY_SCENE`
- `astar`
- `block_layer`
- `on_cell_dug()`
- `update_rail_autotile()`
- `refresh_minecart_paths()`
- wave and spawning behavior

## 8. Documentation updates in the implementation commit

- Update `docs/refactor/VALIDATION_CHECKLIST.md` because it states the executable startup chain and exact active level path.
- Add this frozen implementation manifest.
- Leave broad architecture/audit/planning documents unchanged in this sub-batch unless they contain an executable stale path; their historical root-layout descriptions are not runtime consumers.

## 9. Validation gate

Before commit:

1. Verify the worktree started clean at the recorded base SHA.
2. Review `git diff --summary`, full `git diff`, and `git diff --check`.
3. Confirm exactly one copy of each moved file exists at the target and none at the source.
4. Search tracked files for stale exact runtime paths:
   - `res://level.tscn`
   - `res://world.gd`
5. Search for stale repository-relative direct targets in the explicitly included maintenance files.
6. Confirm no terrain image, `.import`, `.godot`, addon, generated cache, or unrelated asset entered the diff.
7. Confirm the scene UID and script UID are unchanged.
8. Confirm the embedded TileSet source map, atlas declarations, collision data, nodes, properties, and signal connections are unchanged.

Godot validation:

1. Fresh Godot 4.7 headless editor import/check in the clean worktree.
2. Direct load of:
   - `res://scenes/world/mine/level.tscn`
   - `res://scenes/boot/main.tscn`
   - `res://vs_mode.tscn`
   - `res://vs_online.tscn`
   - `res://launch_router.tscn`
3. Bounded configured main-scene runtime launch.
4. Inspect fresh editor/runtime logs for missing resources, parse failures, scene-instantiation errors, TileSet errors, and node-path failures.
5. Run maintained relevant editor tests only if the intentionally untracked Godot AI addon runner is available without entering the commit.

Manual/high-value smoke checks remain required before merging any eventual branch:

- single-player startup and one mining action
- fog/edge/front/damage rendering
- collision and A* update after digging
- gem display/deposit
- rail placement/autotile and minecart path behavior
- Peon navigation contract
- wave spawn
- local VS two-level startup and enemy forwarding

## 10. Rollback

Rollback is the inverse of this one sub-batch only:

- restore `level.tscn`, `world.gd`, and `world.gd.uid` to the repository root
- restore every path listed in sections 3 and 8
- leave all terrain resources at their unchanged original paths
- regenerate ignored Godot cache/import state as needed
- rerun the complete validation gate

Do not reset, clean, stash, or overwrite unrelated work. Do not merge into `main` without explicit approval.

## 11. Local validation results

- Godot 4.7 headless editor project scan/import: passed with exit code 0 and no parser/import errors.
- Bounded headless startup: project remained running until the 10-second timeout; forced termination produced shutdown allocator/thread diagnostics but no scene-load or missing-resource error.
- Terrain assets, embedded TileSet values, node hierarchy, layer ordering, signals, and gameplay logic were not edited.
- Remote push and merge were not performed.
